
{% extends 'base.html.twig' %}
{% block title %}salut BlogController!{% endblock %}

{% block body %}


<div class="header">
  <h2>{{ article.titre}}</h2>
</div>


<div class="container2" style="display: flex; flex-wrap: wrap; justify-content: space-between;
">

  
      <img class="card-img" src={{ article.image}} >
  
      <div class="card-body">
        <h5 class="card-title">{{ article.titre}}</h5>
        <p class="card-text">{{ article.description}}</p>
        <p class="card-text"><small class="text-muted">{{ article.datedeposte | date('d/m/Y')}}</small></p>
      </div>
                      <a class="nav-link" href="{{ path('supprimer',{'id':article.id})}}" style="background-color:lightblue;color:red;">Suprimer l'article !! <span class="sr-only">(current)</span></a>

                      <a class="nav-link" href="{{ path('formulaire',{'id':article.id})}}" style="background-color:lightblue;color:red;">Modifier l'article !! <span class="sr-only">(current)</span></a>


 </div>

{% endblock %}